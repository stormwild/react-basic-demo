scalar GraphQLDate

input IssueInputs {
  title: String!
  status: String
  owner: String
  effort: Int
  due: GraphQLDate
}

type Issue {
  id: Int!
  title: String!
  status: String!
  owner: String
  effort: Int
  created: GraphQLDate!
  due: GraphQLDate
}

type Query {
  about: String!
  issueList: [Issue!]!
}

type Mutation {
  setAboutMessage(message: String!): String
  addIssue(issue: IssueInputs!): Issue!
}
